# ── KTS Option A3: Word + Images (lightweight) ───────────────
# Excludes: PyMuPDF (PDF), python-pptx (PPTX), Pillow (PPTX dep)
# Supported formats: DOCX, HTML, MD, TXT, JSON, YAML, INI, CSV, PNG
# Image extraction: DOCX embedded images only

# ── Core CLI & Utilities ──────────────────────────────────────
click>=8.0.0
tqdm>=4.65.0
PyYAML>=6.0

# ── Document Converters (A3 subset) ──────────────────────────
python-docx>=0.8.11          # DOCX (import docx)
beautifulsoup4>=4.10.0       # HTML (import bs4)
# PyMuPDF — EXCLUDED (PDF support removed in A3)
# python-pptx — EXCLUDED (PPTX support removed in A3)

# ── Build & Packaging (dev only) ─────────────────────────────
pyinstaller>=5.0
setuptools>=65.0

# ── Development & Testing ────────────────────────────────────
pytest>=7.0.0
