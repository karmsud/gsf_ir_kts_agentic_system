# Minimal recommended dependencies for the GraphRAG project
# Install with: pip install -r requirements.txt
# rapidfuzz and openai are optional; the app works without them but with reduced functionality.

streamlit>=1.20
networkx>=2.8
numpy>=1.24
# Optional, faster fuzzy-matching fallback
rapidfuzz>=2.0
# Optional, only needed if you want OpenAI LLM integration
openai>=0.27.0
