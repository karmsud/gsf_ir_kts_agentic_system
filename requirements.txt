
# ── Core CLI & Utilities ──────────────────────────────────────
click>=8.0.0
tqdm>=4.65.0
PyYAML>=6.0
networkx>=3.0.0              # Graph Analysis

# ── Retrieval & Vector Store ─────────────────────────────────
chromadb>=0.4.0              # Local Vector DB + Embeddings
onnxruntime>=1.14.0          # Runtime for default embeddings

# ── Document Converters ──────────────────────────────────────
PyMuPDF>=1.20.0              # PDF  (import fitz)
python-docx>=0.8.11          # DOCX (import docx)
python-pptx>=0.6.21          # PPTX (import pptx)
beautifulsoup4>=4.10.0       # HTML (import bs4)
Pillow>=9.0.0                # Image processing (transitive dep)
lxml>=4.9.0                  # XML parsing for Office docs (transitive dep)

# ── Build & Packaging (dev only) ─────────────────────────────
pyinstaller>=5.0
setuptools>=65.0

# ── Phase 4: Advanced RAG Intelligence ──────────────────────
spacy>=3.7.0                 # NER + noun-chunk keyphrase extraction
tokenizers>=0.15.0           # ONNX cross-encoder tokenization

# ── Development & Testing ────────────────────────────────────
pytest>=7.0.0
